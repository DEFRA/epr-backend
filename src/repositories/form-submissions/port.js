/**
 * @typedef {Object} FormSubmissionsRepository
 * @property {() => Promise<Object[]>} findAllAccreditations
 * @property {() => Promise<Object[]>} findAllRegistrations
 */

/**
 * @typedef {() => FormSubmissionsRepository} FormSubmissionsRepositoryFactory
 */
