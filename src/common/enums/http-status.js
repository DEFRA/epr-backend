/**
 * HTTP status code constants
 *
 * SonarCloud flags magic numbers as code smells because they lack semantic meaning.
 * These constants provide clear intent and make code more readable.
 */

export const HTTP_STATUS = {
  OK: 200,
  ACCEPTED: 202
}
